name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"
port = 8080

[commands]
# Prisma generate - if EPERM (file locked), continues (client may already exist)
predev = "npx prisma generate || echo"
dev = "npx prisma migrate deploy && npm exec react-router dev"
